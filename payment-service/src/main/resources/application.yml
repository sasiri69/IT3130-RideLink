server:
  port: ${PORT:8084}

spring:
  application:
    name: payment-service
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/payment_db}

services:
  account:
    url: ${SERVICES_ACCOUNT_URL:http://localhost:8081}
    timeout-ms: 5000
    bypass-on-unreachable: ${SERVICES_ACCOUNT_BYPASS:true}
  ride:
    url: ${SERVICES_RIDE_URL:http://localhost:8083}
    timeout-ms: 5000
    bypass-on-unreachable: ${SERVICES_RIDE_BYPASS:true}

jwt:
  secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
  expiration: ${JWT_EXPIRATION:86400000}

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
